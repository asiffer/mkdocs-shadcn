---
title: Internationalization
summary: Add theme translations
---

The theme supports the `lang` attribute. 
This page aims to describe how to add additional translations.

First [setup the project](./20_setup.md).

Create a specific directory for your lang (see [ISO639](https://www.localeplanet.com/icu/iso639.html) for the full list).

```shell
mkdir -p shadcn/locales/<lang>/LC_MESSAGES
```

Generates a `messages.po` in that directory (it may also update other langs if new tokens have been added):
```shell
uv run ./manage.py makemessages
```

!!! info ""
    The `manage.py` script is a dev CLI tool with several commands that ease developer tasks.

Edit translations in `shadcn/locales/<lang>/LC_MESSAGES/messages.po` (the file previously generated). You can look at the [`fr`](https://github.com/asiffer/mkdocs-shadcn/blob/master/shadcn/locales/fr/LC_MESSAGES/messages.po) example.


Compile the translations (it generates `messages.mo`) with
```shell
uv run ./manage.py compilemessages
```